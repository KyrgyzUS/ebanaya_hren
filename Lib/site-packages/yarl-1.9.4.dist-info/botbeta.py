import sqlite3

# Connect to SQLite database (or create it if it doesn't exist)
conn = sqlite3.connect('bot_data.db')

# Create a cursor object
cursor = conn.cursor()

# Create a table
cursor.execute('''
CREATE TABLE IF NOT EXISTS user_questions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chat_id INTEGER NOT NULL,
    question TEXT NOT NULL,
    date TEXT NOT NULL,
    time TEXT NOT NULL
)
''')

# Commit the changes and close the connection
conn.commit()
conn.close()

import openai
from aiogram import Bot, Dispatcher, types
from aiogram.utils import executor
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
from openai import AsyncOpenAI, OpenAIError
import gspread
from google.oauth2.service_account import Credentials
from googleapiclient.discovery import build
from datetime import datetime
import asyncio

# Ваш токен бота Telegram
TELEGRAM_API_TOKEN = '7130675357:AAH-rJDqmG54sd1HnryRCwtDygRJb25hiyA'
# Ваш API-ключ OpenAI
OPENAI_API_KEY = 'sk-proj-vbp5XiLBUs9LU1r6qPaWT3BlbkFJrQQwKSHs89x9zwu8T582'
# Путь к вашему JSON файлу с учетными данными Google
SERVICE_ACCOUNT_FILE = 'C:\\RENDERS ANIM\\pythonProject4\\iron-figure-424308-d0-5c09bc12ac78.json'

bot = Bot(token=TELEGRAM_API_TOKEN)
dp = Dispatcher(bot)

client = AsyncOpenAI(api_key=OPENAI_API_KEY)

SCOPES = ['https://www.googleapis.com/auth/spreadsheets', 'https://www.googleapis.com/auth/drive']
creds = Credentials.from_service_account_file(SERVICE_ACCOUNT_FILE, scopes=SCOPES)
gspread_client = gspread.authorize(creds)

# Google Drive API клиент
drive_service = build('drive', 'v3', credentials=creds)

# ID исходной таблицы
source_spreadsheet_id = '1ahBGla54O1op6DchLZ10d_4JMIEYQJtxGgIPp89JFms'

# Пример базы знаний в виде списка ответов
knowledge_base = [
    "|ТК 'ALPHA cargo' цена доставка Москва: Пошив - 1кг 50₽, Привозные - 1кг 55₽, Маркировка - 1кг 65₽, Бренд - 1кг 65₽, Адрес склада в Москве :Г. Москва Джержинский ул Энеркетиков  30 строение 2, Сроки доставки 5-7 дней, Если товар не превышает 10кг. то ставится фиксированная стоимость доставки 1000 ₽"

    "|Цена доставка Екатеринбург: Пошив-1кг 50р, Привозные-1кг 50р, Маркировка-1кг 65р, Бренд-1кг 65р, Сроки доставки +- 8 дней"

    "|В Узбекистан и Таджикистан товар отправляем через частные карго (абу-сахи). Доставка стоит 3,5$ за один килограмм если вес груза не достигает 20кг. Если товар весит больше  20кг доставка за один килограмм будет стоить 3$. Сроки доставки +-7 дней."

    "|Цена доставка карго до УФЫ: Прайс за 1кг.: Пошив, Ткани - 40 рублей Китай - 50 рублей Маркировка - 50 рублей (менее 50кг за 1 место-70 рублей) Бренд - 50 рублей Турция - 60 рублей"

    "|Цена доставка Самара: Пошив - 1кг 55₽ Привозные - 1кг 55₽ Маркировка - 1кг 65₽ Бренд - 1кг 65₽ Обувь - 1кг 100₽. Сроки доставки 6-7дней. Если товар не превышает 10кг. то ставится фиксированная стоимость доставки 1000 ₽"

    "|Цена доставка Казань: Пошив - 1кг 45₽ Привозные - 1кг 55₽ Маркировка - 1кг 65₽ Бренд - 1кг 65₽ Сроки доставки 7-8 дней. Если товар не превышает 10кг. то ставится фиксированная стоимость доставки 1000 ₽"

    "|Цена доставка Красноярск: Пошив-1кг 55р Привозные-1кг 65р Маркировка-1кг 85р Бренд-1кг 85р Обувь-1кг 105р. г.Красноярск, проспект Газеты «Красноярский Рабочий» 27 строение 70, ТК «Восточный»."

    "|Прямая доставка РоссКарго. Г. Ростов-на-Дону. Пошив 1кг - 55 ₽ Китай, Турция - 70 ₽ Бренд, Маркир 1кг - 70₽ Спец. форма 1кг - 60₽ Обувь 1кг - 100₽ Сроки доставки +-7дней"

    "|Доставка по Казахстану: частные перевозчики, западный Казахстан ±3-5 дней, центральный Казахстан ±1-4 дня, восточный Казахстан 1-3 дня. Цена зависит от объема."

    "|Обувь выкупаем и отправить можем , но при доставке в ТК коробку либо саму обувь могут немного помять. И уточняйте тарифы у админа  , на обувь цена больше за доставку из за объема"

    "|В Дагестан, Сочи, Махачкалу, Пятигорск, Хасавюрт, Грозный, Чечню груз отправлем через Москву"

    "|курс рубля = 0,95 сома. Курс доллара = 86 сом. Курс тенге = 0.196 сома."

    "|Честный знак сделать возможно он есть. Прайс (цена) фуллфилмента (ФФ): https://docs.google.com/spreadsheets/d/1C7Ik1OxD_Bsrkb43IqaBNK_QHQw9Byk8ZcnYVJ58Y0A/edit?usp=drivesdk"

    "|Условия заказа / комиссия за услуги: (организационный сбор). Нет ограничения по сумме, от 1-упаковок (линией). До $99 - фиксированная комиссия 600 сом. От $140 - 6% от суммы перевода (не израсходованный остаток - не учитывается). От $999 - торгуемо (учитывается сложность заказа) 🤝. Выше $1999 - на усмотрение клиента в нижних пределах рыночных цен 🥳. Дополнительно: Сохранность денежных средств и выкупленного товара, до момента сдачи перевозчику (если перевозчик согласован с заказчиком) гарантируется коллективом (команда из более 150 человек) Дордой Фэшн - только в случае перевода / исполнения заказа в рабочем чате заказчика (WhatsApp & Telegram) в присутствии номера wa.me/996500996500"

    "|ТК ALPHA cargo цена доставка Новосибирск: Пошив - 1кг 50₽, Привозные - 1кг 55₽, Маркировка - 1кг 65₽, Бренд - 1кг 65₽, Адрес: г.Новосибирск, Территория СНТ Весна Сибири,65 ТК «Восток», Сроки доставки 7-8 дней, Если товар не превышает 10кг. то ставится фиксированная стоимость доставки 1000 ₽"

    "Если у вас возникнут дополнительные вопросы, не стесняйтесь обратиться к администратору по номеру +996 705 705 996."
]

# Глобальные переменные для хранения данных пользователя
user_data = {}

def save_question_to_db(chat_id, question):
    conn = sqlite3.connect('bot_data.db')
    cursor = conn.cursor()
    date = datetime.now().strftime('%Y-%m-%d')
    time = datetime.now().strftime('%H:%M:%S')
    cursor.execute('''
    INSERT INTO user_questions (chat_id, question, date, time)
    VALUES (?, ?, ?, ?)
    ''', (chat_id, question, date, time))
    conn.commit()
    conn.close()

def get_all_questions():
    conn = sqlite3.connect('bot_data.db')
    cursor = conn.cursor()
    cursor.execute('SELECT chat_id, question, date, time FROM user_questions')
    rows = cursor.fetchall()
    conn.close()
    return rows


@dp.message_handler(commands=['start'])
async def send_welcome(message: types.Message):
    keyboard = InlineKeyboardMarkup()
    create_button = InlineKeyboardButton("Cоздать счет-фактуру", callback_data="create_table")
    keyboard.add(create_button)
    await message.reply("Привет! Нажмите на кнопку ниже, чтобы создать таблицу. Или напишите мне, чтобы узнать о доставке или других моментах", reply_markup=keyboard)

@dp.callback_query_handler(lambda c: c.data == 'create_table')
async def process_callback_create_table(callback_query: types.CallbackQuery):
    user_id = callback_query.from_user.id
    await bot.send_message(user_id, "Счет-фактура создается...")
    try:
        # Копирование исходной таблицы
        copy_title = "Copy of Source Spreadsheet"
        copied_file = drive_service.files().copy(
            fileId=source_spreadsheet_id,
            body={"name": copy_title}
        ).execute()

        new_spreadsheet_id = copied_file['id']

        # Сделать новую таблицу доступной для всех
        permission = {
            'type': 'anyone',
            'role': 'reader',
        }
        drive_service.permissions().create(
            fileId=new_spreadsheet_id,
            body=permission
        ).execute()

        # Сохранение идентификатора новой таблицы для пользователя
        user_data[user_id] = {'spreadsheet_id': new_spreadsheet_id}

        await bot.send_message(user_id, "Таблица создана. Пожалуйста, введите имя клиента.", reply_markup=cancel_keyboard())
    except Exception as e:
        await bot.send_message(callback_query.from_user.id, f"Произошла ошибка: {e}")

def cancel_keyboard():
    keyboard = InlineKeyboardMarkup()
    cancel_button = InlineKeyboardButton("Cancel", callback_data="cancel")
    keyboard.add(cancel_button)
    return keyboard

@dp.callback_query_handler(lambda c: c.data == 'cancel')
async def cancel_process(callback_query: types.CallbackQuery):
    user_id = callback_query.from_user.id
    if user_id in user_data and 'spreadsheet_id' in user_data[user_id]:
        try:
            drive_service.files().delete(fileId=user_data[user_id]['spreadsheet_id']).execute()
            await bot.send_message(user_id, "Процесс отменен и таблица удалена.")
        except Exception as e:
            await bot.send_message(user_id, f"Не удалось удалить таблицу: {e}")
    else:
        await bot.send_message(user_id, "Нет активного процесса для отмены.")
    user_data.pop(user_id, None)

@dp.message_handler(lambda message: message.from_user.id in user_data and 'first_name' not in user_data[message.from_user.id])
async def process_first_name(message: types.Message):
    user_id = message.from_user.id
    user_data[user_id]['first_name'] = message.text
    await message.reply("Пожалуйста, введите город клиента.", reply_markup=cancel_keyboard())

@dp.message_handler(lambda message: message.from_user.id in user_data and 'last_name' not in user_data[message.from_user.id])
async def process_last_name(message: types.Message):
    user_id = message.from_user.id
    user_data[user_id]['last_name'] = message.text
    await message.reply("Пожалуйста, введите имя менеджера.", reply_markup=cancel_keyboard())

@dp.message_handler(lambda message: message.from_user.id in user_data and 'manager_name' not in user_data[message.from_user.id])
async def process_manager_name(message: types.Message):
    user_id = message.from_user.id
    user_data[user_id]['manager_name'] = message.text

    # Переименование файла
    first_name = user_data[user_id]['first_name']
    last_name = user_data[user_id]['last_name']
    manager_name = user_data[user_id]['manager_name']
    today = datetime.today().strftime('%d.%m.%Y')
    new_title = f"{first_name} {last_name} - {manager_name} {today}"

    drive_service.files().update(
        fileId=user_data[user_id]['spreadsheet_id'],
        body={"name": new_title}
    ).execute()

    new_spreadsheet_url = f"https://docs.google.com/spreadsheets/d/{user_data[user_id]['spreadsheet_id']}/edit"
    await message.reply(f"Таблица переименована и доступна для всех: {new_spreadsheet_url}")
    user_data.pop(user_id)  # Очистка данных пользователя

@dp.message_handler(commands=['allquestions'])
async def send_all_questions(message: types.Message):
    questions = get_all_questions()
    if not questions:
        await message.reply("Вопросы не найдены.")
        return

    response = "Все вопросы:\n"
    for chat_id, question, date, time in questions:
        response += f"Chat ID: {chat_id}\nВопрос: {question}\nДата: {date}\nВремя: {time}\n\n"

    await message.reply(response)

@dp.message_handler()
async def handle_message(message: types.Message):
    user_question = message.text.lower()
    response = await get_response(user_question)
    await message.reply(response)

@dp.message_handler()
async def handle_message(message: types.Message):
    user_question = message.text.lower()
    save_question_to_db(message.chat.id, user_question)  # Сохранение вопроса в базу данных
    response = await get_response(user_question)
    await message.reply(response)

async def get_response(question):
    # Создание контекста на основе базы знаний
    knowledge_base_content = "\n".join([f"Ответ: {a}" for a in knowledge_base])
    messages = [
        {"role": "system", "content": "Ты ассистент. Используй следующую базу знаний для ответа на вопросы. Доставка только в города России, Казахстана, Узбекистана и Беларуссии иначе скажи 'Проконсультируйтесь у Администратора +996 705 705 996' далее если у тебя спросят про доставку в город отсутствующий в базе но доступной стране, то скажи что доставка в этот город осуществляется через ближайший из городов (географически) в базе знаний 'Доставка в (запрашиваемый город) осуществляется через ближайший город сотрудниками ТК (ближайший город)' и вышли информацию о ней всю а послк допиши 'досыл по тарифам местных ТК'. Если ты не найдешь ответ ответь 'Проконсультируйтесь у Администратора +996 705 705 996' "},
        {"role": "user", "content": f"База знаний:\n{knowledge_base_content} Вопрос: {question}\n"}
    ]
    return await get_openai_response(messages)

async def get_openai_response(messages):
    try:
        response = await client.chat.completions.create(
            model="gpt-3.5-turbo",
            messages=messages
        )
        return response.choices[0].message.content.strip()
    except OpenAIError as e:
        if e.code == 'insufficient_quota':
            return "Произошла ошибка при запросе к OpenAI API: превышена квота использования. Пожалуйста, проверьте план и детали биллинга."
        return f"Произошла ошибка при запросе к OpenAI API: {e}"

async def on_startup(dp):
    while True:
        try:
            await dp.start_polling()
        except (asyncio.TimeoutError, aiohttp.ClientConnectionError, aiohttp.ClientPayloadError, aiohttp.ServerDisconnectedError) as e:
            print(f"Network error occurred: {e}. Restarting polling...")
            await asyncio.sleep(5)

async def on_startup(dp):
    while True:
        try:
            await dp.start_polling()
        except (asyncio.TimeoutError, aiohttp.ClientConnectionError, aiohttp.ClientPayloadError, aiohttp.ServerDisconnectedError) as e:
            print(f"Network error occurred: {e}. Restarting polling...")
            await asyncio.sleep(5)
        except Exception as e:
            print(f"An unexpected error occurred: {e}. Restarting polling...")
            await asyncio.sleep(5)

if __name__ == '__main__':
    executor.start_polling(dp, skip_updates=True, on_startup=on_startup)
